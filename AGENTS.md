# 저장소 작업 지침

## 언어

- 프로젝트가 직접 관리하는 README, 설계 문서, 운영 문서, 코드 주석, 설정 주석은 한국어로
  작성합니다.
- 패키지명, API 식별자, 환경 변수, 명령어, 파일 경로는 원래 표기를 유지합니다.
- 외부 도구가 자동 생성하는 파일은 직접 번역하지 않습니다. 언어를 바꿔야 한다면 생성
  도구의 설정이나 원본 템플릿을 수정합니다.

## 작업 범위

- 요청한 결과를 만드는 가장 작은 변경부터 구현합니다. 현재 요구에 사용되지 않는 추상화,
  확장 지점, 패키지, 의존성을 미리 추가하지 않습니다.
- 작업을 시작할 때 최신 `origin/main`과 현재 변경 상태를 확인합니다. 사용자가 만든 변경과
  관련 없는 파일은 수정하거나 되돌리지 않습니다.
- 하나의 변경은 하나의 검토 가능한 목적에 집중합니다. 요구사항이 제품 동작이나 작업 범위를
  실질적으로 바꾸는 경우에는 임의로 결정하지 않고 사용자에게 확인합니다.
- 여러 앱, 공용 계약, 인증, 데이터 또는 배포 정책에 영향을 주는 작업은 구현 전에 Spec Kit으로
  요구사항과 계획을 작성합니다. 작은 설정 변경이나 명확한 버그 수정에는 사용하지 않습니다.

## 모노레포 경계

- `apps/*`는 독립적으로 실행하거나 배포하는 제품 단위이고, `packages/*`는 재사용 가능한 계약과
  기반 기능입니다.
- 앱은 패키지에 의존할 수 있지만 패키지는 앱에 의존하지 않습니다. 앱 사이에서 코드를 직접
  import하지 않습니다.
- 다른 워크스페이스의 내부 파일을 직접 import하지 않고 해당 패키지의 공개 진입점을 사용합니다.
- 공용 코드는 둘 이상의 실제 소비자가 있거나 API, 브리지, 디자인 토큰처럼 저장소 차원의 안정된
  계약일 때만 패키지로 옮깁니다.
- 공통 외부 의존성은 pnpm catalog에서 관리하고 내부 의존성은 `workspace:*`로 선언합니다.

## 생성 코드와 계약

- OpenAPI 클라이언트와 `apps/admin/src/routeTree.gen.ts`처럼 도구가 생성하는 파일은 직접
  수정하지 않습니다. 생성 설정, 원본 명세 또는 템플릿을 수정한 뒤 다시 생성합니다.
- API 명세와 `@yapp-plus/app-bridge` 타입을 시스템 경계의 단일 진실 공급원으로 사용합니다.
- `any`, 무검증 타입 단언, 오류 무시 지시어는 더 안전한 표현이 불가능할 때만 제한적으로 사용하고
  이유를 코드 또는 PR에 기록합니다.
- 외부 입력, 환경 변수와 브리지 메시지는 신뢰 경계에서 검증합니다.

## 도구와 의존성

- Node.js와 pnpm은 `mise.toml`, `mise.lock`, `package.json`에 고정된 버전을 사용합니다.
- 루트 `package.json`의 script는 `node --run <script>`로 실행합니다. 의존성 설치·변경,
  `pnpm --filter`, `pnpm exec`와 package manager 동작에는 `pnpm`을 사용합니다.
- 새 의존성을 추가하기 전에 기존 워크스페이스와 플랫폼 API로 해결할 수 있는지 확인합니다.
- 의존성 공급망 보호를 전체적으로 비활성화하지 않습니다. 임시 예외는 정확한 패키지와 버전으로
  제한하고 사유와 제거 조건을 주석에 기록합니다.
- 외부 GitHub Action과 배포 CLI는 재현 가능한 버전이나 commit SHA로 고정합니다.

## 검증

- 변경에는 실패 가능성을 가장 가까운 계층에서 확인하는 테스트를 추가합니다. 버그 수정에는 가능한
  경우 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 포함합니다.
- 기본 품질 게이트는 `node --run check`입니다. 배포 가능한 앱이나 빌드 설정을 변경하면
  `node --run build`도 실행합니다.
- 모바일 설정 또는 의존성을 변경하면 `node --run expo:doctor`를 실행합니다.
- 재사용 UI의 동작이나 상태를 변경하면 관련 테스트와 Storybook 사례를 추가하거나 갱신하고,
  키보드·스크린 리더·터치 환경을 확인합니다.
- 검증을 실행하지 못한 경우 실행한 항목, 실행하지 못한 항목과 남은 위험을 최종 응답에 명시합니다.

## Git과 GitHub

- 저장소 변경을 시작하거나 issue, commit, push, PR, CI 재실행, merge를 다룰 때는
  [GitHub 작업 흐름](.agents/skills/github-workflow/SKILL.md)을 먼저 읽고 따릅니다.

## 보안과 배포

- 비밀 값, 인증 정보와 개인 정보를 저장소, 빌드 산출물, 로그 또는 응답에 기록하지 않습니다.
- 워크플로와 배포 자동화에는 필요한 최소 권한만 부여합니다.
- 프로덕션 배포는 `main`의 검증된 커밋만 대상으로 하고 배포 결과를 commit SHA와 연결합니다.
- 인증, 데이터, 공용 계약 또는 배포 방식을 변경할 때는 적용 전에 실패 시 복구 또는 롤백 방법을
  정합니다.
